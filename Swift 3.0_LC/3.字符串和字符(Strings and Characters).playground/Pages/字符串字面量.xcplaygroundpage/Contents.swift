//: ## 字符串字面量
//: 你可以在你的代码中插入预先写好的 `String`值作为字符串字面量。字符串字面量是由双引号("") 包裹的固定顺序文本字符集

//: 字符串字面量可以用于为常量和变量提供初始值：
let someString = "Some string literal value"

//:> 注意 Swift 会为 `someString`常量推断类型为 `String`，因为你用了字符串字面量值来初始化。

//: [上一页](@previous) | [下一页](@next)
