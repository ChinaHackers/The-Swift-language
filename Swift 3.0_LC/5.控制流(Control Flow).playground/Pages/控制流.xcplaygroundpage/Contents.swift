//: ## 控制流
//: - 本页包含内容：
//: - For循环      For-In
//: - While循环     while、Repeat-While
//: - 条件语句       if else ...等
//: - 控制转移语句    Swift 有五种控制转移语句:continue, break, fallthrough, return, throw
//: - 提前退出       guard else
//: - 检测API可用性  Swift 有检查 API 可用性的内置支持，这可以确保我们不会不小心地使用对于当前部署目标不可用的 API
//: ----

//: `Swift` 提供了类似 `C` 语言的流程控制结构，包括可以多次执行任务的 `for` 和 `while` 循环，基于特定条件选择执行不同代码分支的`if、guard` 和 `switch` 语句，还有控制流程跳转到其他代码的 `break`和 `continue`语句。


//: [For循环](@next)
