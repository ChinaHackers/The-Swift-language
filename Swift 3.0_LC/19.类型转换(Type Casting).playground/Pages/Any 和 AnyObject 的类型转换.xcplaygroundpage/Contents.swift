//: ## Any 和 AnyObject 的类型转换

/*:
//: `Swift` 为不确定的类型提供了两种特殊的类型别名：
- `AnyObject`  可以表示任何类类型的实例。
- `Any`  可以表示任何类型，包括函数类型。
*/

//:> 注意: 只有当你确切需要使用它们的功能和行为时再使用 `Any` 和 `AnyObject` 。在写代码时使用更加明确的类型表达总要好一些。

//: [上一页](@previous) | [下一页](@next)

